[
  {
    "name": "/update_schedule command shows setup prompt when not configured",
    "steps": [
      {
        "send": { "text": "/update_schedule" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "⚠️ You haven't set up the bot yet. Please run /start to configure your channel and schedule first."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "Update schedule button shows schedule prompt when configured",
    "steps": [
      {
        "send": { "callback": "schedule:update" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
